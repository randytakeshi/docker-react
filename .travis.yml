language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t randytakeshi/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true randytakeshi/docker-react npm run test